{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "env": {
    "GEMINI_API_KEY": {
      "description": "Google Gemini API Key for text analysis",
      "required": true
    }
  }
}
